version: '3.8'

services:
  brush-flow:
    build: .
    container_name: brush-flow
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - BF_DEBUG=false
      - BF_HOST=0.0.0.0
      - BF_PORT=8000
    restart: unless-stopped
