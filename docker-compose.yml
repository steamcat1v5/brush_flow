version: '3.8'

services:
  brush-flow:
    build: .
    container_name: brush-flow
    ports:
      - "${BF_PORT:-8765}:${BF_PORT:-8765}"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
