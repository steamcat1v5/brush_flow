version: '3.8'

services:
  brush-flow:
    build: .
    container_name: brush-flow
    ports:
      - "${BF_PORT:-8000}:${BF_PORT:-8000}"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
